# the demo ~~


# import numpy as np
# import matplotlib.pyplot as plt

# m = np.array(range(16))+1
# resi = np.resize(m,(4,4)) # resize 是copy, 深复制
# resh = np.reshape(m, (4,4)) # reshape 是切片, 浅复制
# m[0] = 1000
# print(np.linalg.inv(resi))
# print(resh.max(1))
# # plt.plot(m[0], m[1])
# # plt.show()